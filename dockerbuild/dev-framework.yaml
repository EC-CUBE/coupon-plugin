# 開発環境(HTTP)で管理画面にログインできるよう Cookie 設定を緩和する。
# EC-CUBE 4.4 / Symfony 7 では既定が cookie_samesite: none のため、
# HTTP 接続ではブラウザが Cookie を拒否しログインできない。
# (本体の install / codeception 環境と同じ上書きを dev にも適用する)
framework:
    session:
        cookie_secure: false
        cookie_samesite: lax
